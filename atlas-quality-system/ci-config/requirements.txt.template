# Atlas项目 requirements.txt 模板
# 适用:如果Atlas后端使用Python(FastAPI等)
# 如果Atlas后端是Node.js,忽略本文件

# ============================================================
# 注意:版本号建议固定到minor(允许patch更新),避免意外升级
# 例如 fastapi>=0.115,<0.116
# ============================================================

# ============================================================
# Web框架
# ============================================================
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.31.0
gunicorn>=23.0.0,<24.0.0  # 生产环境进程管理

# ============================================================
# 数据验证
# ============================================================
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
email-validator>=2.2.0,<3.0.0

# ============================================================
# 数据库
# ============================================================
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0  # 数据库迁移
asyncpg>=0.29.0,<0.30.0  # PostgreSQL异步驱动
psycopg2-binary>=2.9.0,<3.0.0  # PostgreSQL同步驱动(如需要)

# ============================================================
# 缓存
# ============================================================
redis>=5.0.0,<6.0.0
hiredis>=3.0.0,<4.0.0  # 可选,redis的C加速器

# ============================================================
# HTTP客户端(调用外部API)
# ============================================================
httpx>=0.27.0,<0.28.0
tenacity>=9.0.0,<10.0.0  # 重试机制

# ============================================================
# 认证与安全
# ============================================================
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT
passlib[bcrypt]>=1.7.0,<2.0.0  # 密码哈希
python-multipart>=0.0.9  # 文件上传
slowapi>=0.1.9,<0.2.0  # 速率限制

# ============================================================
# 日志与监控
# ============================================================
structlog>=24.4.0,<25.0.0  # 结构化日志
sentry-sdk[fastapi]>=2.13.0,<3.0.0  # 错误追踪
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0  # 指标监控

# ============================================================
# 任务队列(如需异步任务)
# ============================================================
celery>=5.4.0,<6.0.0
redis>=5.0.0,<6.0.0  # Celery broker

# ============================================================
# 工具库
# ============================================================
python-dateutil>=2.9.0,<3.0.0
pytz>=2024.2
ujson>=5.10.0,<6.0.0  # 更快的JSON
orjson>=3.10.0,<4.0.0  # 最快的JSON

# ============================================================
# 配置管理
# ============================================================
python-dotenv>=1.0.0,<2.0.0

# ============================================================
# Feature Flags
# ============================================================
UnleashClient>=6.0.0,<7.0.0

# ============================================================
# 开发依赖(分开为 requirements-dev.txt 管理更佳)
# ============================================================

# 测试
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<0.25.0
pytest-cov>=5.0.0,<6.0.0
pytest-xdist>=3.6.0,<4.0.0  # 并行测试
pytest-mock>=3.14.0,<4.0.0
pytest-benchmark>=4.0.0,<5.0.0
pytest-timeout>=2.3.0,<3.0.0
hypothesis>=6.112.0,<7.0.0  # 属性测试
faker>=28.4.0,<29.0.0  # 测试数据生成
factory-boy>=3.3.0,<4.0.0  # 工厂模式

# 集成测试
testcontainers[postgres,redis]>=4.8.0,<5.0.0
respx>=0.21.0,<0.22.0  # httpx的mock

# Lint与类型
ruff>=0.6.0,<0.7.0  # 快速lint
mypy>=1.11.0,<2.0.0  # 类型检查
bandit[toml]>=1.7.0,<2.0.0  # 安全扫描
safety>=3.2.0,<4.0.0  # 依赖漏洞扫描

# Mutation测试
mutmut>=2.5.0,<3.0.0

# 性能测试
locust>=2.31.0,<3.0.0

# 调试
ipython>=8.27.0,<9.0.0
ipdb>=0.13.0,<0.14.0
rich>=13.8.0,<14.0.0  # 美化输出

# 文档
mkdocs>=1.6.0,<2.0.0  # 如需要文档站
mkdocs-material>=9.5.0,<10.0.0

# pre-commit
pre-commit>=3.8.0,<4.0.0

# ============================================================
# 业务人员说明
# ============================================================
#
# 1. 安装依赖:
#    pip install -r requirements.txt
#
# 2. 推荐使用虚拟环境:
#    python -m venv .venv
#    source .venv/bin/activate  # macOS/Linux
#    .venv\Scripts\activate     # Windows
#    pip install -r requirements.txt
#
# 3. 推荐使用 poetry 或 uv 管理依赖(更现代)
#
# 4. 添加新依赖:
#    a. 先用《架构守护提示词》评估必要性
#    b. 选择维护活跃、漏洞少的库
#    c. 固定版本号
#    d. 提交PR(CODEOWNERS会要求审查)
#
# 5. 升级依赖:
#    a. 先在Staging测试
#    b. 大版本升级要看Changelog
#    c. 配合 pip-audit / safety 检查漏洞
